"""
Stage 2: LLM 规则生成 — Prompt 模板库

定义与 LLM 交互的各类 Prompt 模板，将 GNN 结构化洞察转化为可执行风控规则。
"""

# ============================================================
# System Prompt: 定义 LLM 的角色和输出格式
# ============================================================
SYSTEM_PROMPT = """你是一位资深的金融风控专家，专门负责移动支付场景下的反欺诈规则设计。

你的任务是基于图神经网络 (GNN) 检测到的异常模式，自动生成可落地的风控检测规则。

## 输出格式要求
你必须以 JSON 数组格式输出规则，每条规则包含以下字段：
```json
{
    "rule_id": "R-YYYYMMDD-NNN",
    "name": "规则名称 (简洁描述)",
    "description": "详细的规则说明，解释检测逻辑和适用场景",
    "risk_type": "风险类型 (如: 集团欺诈/洗钱/盗刷/薅羊毛)",
    "severity": "high/medium/low",
    "conditions": [
        {
            "field": "特征字段名",
            "operator": "> / < / >= / <= / == / != / in / between",
            "value": "阈值或范围"
        }
    ],
    "logic": "AND / OR (条件之间的逻辑关系)",
    "action": "block / review / alert / limit",
    "explanation": "规则生成的推理依据 (基于哪个 GNN 异常模式)"
}
```

## 规则设计原则
1. **精准性**: 规则条件应尽量精确，避免过度宽泛导致误杀
2. **可解释性**: 每条规则必须有清晰的业务解释
3. **可执行性**: 规则条件必须基于数据中已有的特征字段
4. **分层防御**: 根据风险等级设置不同的处置动作
5. **兼顾召回与精度**: 优先保证召回率，同时控制误杀率 < 5%
"""

# ============================================================
# 规则生成 Prompt: 基于 GNN 风险洞察生成规则
# ============================================================
RULE_GENERATION_PROMPT = """## 当前任务
基于以下 GNN 检测到的风险模式，生成相应的风控检测规则。

## GNN 风险洞察
{risk_insights}

## 数据集特征说明
- 数据集: {dataset_name}
- 总节点数: {total_nodes}
- 特征维度: {feature_dims}
- 异常比例: {anomaly_ratio}

## 历史参考规则 (来自知识库 RAG 检索)
{historical_rules}

## 约束条件
- 每个风险模式至少生成 1 条规则，最多 3 条
- 条件中的特征字段用 feature_dim_X 表示 (X 为特征维度索引)
- 度相关特征用 node_degree 表示
- 请确保覆盖所有检测到的风险聚类

请生成规则 JSON 数组：
"""

# ============================================================
# 规则优化 Prompt: 基于沙盒回测结果优化规则
# ============================================================
RULE_OPTIMIZATION_PROMPT = """## 当前任务
以下规则在沙盒回测中表现不佳，请基于回测结果进行优化。

## 待优化规则
{rules_to_optimize}

## 回测结果
{evaluation_results}

## 优化方向
- 如果召回率低 (< 50%): 考虑放宽条件阈值或增加 OR 条件
- 如果误杀率高 (> 5%): 考虑收紧条件阈值或增加 AND 条件
- 如果覆盖范围不足: 考虑针对未覆盖的异常模式新增规则

## 历史有效规则参考
{historical_rules}

请输出优化后的规则 JSON 数组：
"""

# ============================================================
# 规则解释 Prompt: 为规则生成可读的风控报告
# ============================================================
RULE_EXPLANATION_PROMPT = """## 当前任务
请为以下风控规则生成面向运营人员的中文解释报告。

## 规则列表
{rules}

## 报告要求
对每条规则生成:
1. **风险场景**: 这条规则针对什么样的风险?
2. **检测逻辑**: 用通俗语言解释规则条件
3. **处置建议**: 触发规则后应如何处理
4. **注意事项**: 可能的误杀场景和豁免条件

请以 Markdown 格式输出：
"""
