"""
Stage 2: LLM 规则生成 — Prompt 模板库

定义与 LLM 交互的各类 Prompt 模板，将 GNN 结构化洞察转化为可执行风控规则。
"""

# ============================================================
# System Prompt: 定义 LLM 的角色和输出格式
# ============================================================
SYSTEM_PROMPT = """你是一位资深的金融风控专家，专门负责移动支付场景下的反欺诈规则设计。

你的任务是基于图神经网络 (GNN) 检测到的异常模式，自动生成可落地的风控检测规则。

## 输出格式要求
你必须以纯 JSON 数组格式输出规则（不要包含 markdown 代码块标记），每条规则包含以下字段：
[
    {
        "name": "规则名称 (简洁描述)",
        "description": "详细的规则说明，解释检测逻辑和适用场景",
        "risk_type": "风险类型 (如: 集团欺诈/洗钱/盗刷/薅羊毛)",
        "severity": "high/medium/low",
        "conditions": [
            {
                "field": "特征字段名 (必须是下方可用字段之一)",
                "operator": "> / < / >= / <= / == / !=",
                "value": 数值阈值
            }
        ],
        "logic": "AND / OR (条件之间的逻辑关系)",
        "action": "block / review / alert / limit",
        "explanation": "规则生成的推理依据 (基于哪个 GNN 异常模式)"
    }
]

## 可用字段 (规则条件只能使用以下字段)
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `node_degree` | float | 节点度数，即该节点的连接数/交易对手数 |
| `node_degree_zscore` | float | 节点度数的标准分数，>2 表示远高于平均水平 |
| `feature_dim_X` | float | 第 X 维原始特征值 (X 从 0 开始) |
| `feature_dim_X_zscore` | float | 第 X 维特征的标准分数，|z|>1.5 表示显著偏离全局均值 |

注意：zscore 字段表示该值偏离全局均值多少个标准差，正值=偏高，负值=偏低。

## 规则设计原则
1. **精准性**: 规则条件应尽量精确，避免过度宽泛导致误杀
2. **可解释性**: 每条规则必须有清晰的业务解释
3. **可用字段限制**: 只能使用上方列出的字段，不要使用其他字段名
4. **阈值参考**: 请参考下方提供的数据分布统计来设定合理的阈值
5. **兼顾召回与精度**: 优先保证召回率，同时控制误杀率 < 5%
"""

# ============================================================
# 规则生成 Prompt: 基于 GNN 风险洞察生成规则
# ============================================================
RULE_GENERATION_PROMPT = """## 当前任务
基于以下 GNN 检测到的风险模式，生成相应的风控检测规则。

## GNN 风险洞察
{risk_insights}

## 数据集基本信息
- 数据集: {dataset_name}
- 总节点数: {total_nodes}
- 特征维度: {feature_dims}
- 异常比例: {anomaly_ratio}

## 数据分布统计 (用于设定合理阈值)
{data_distribution}

## 历史参考规则 (来自知识库 RAG 检索)
{historical_rules}

## 约束条件
- 每个风险模式至少生成 1 条规则，最多 3 条
- 条件中的 field 只能使用: node_degree, node_degree_zscore, feature_dim_X, feature_dim_X_zscore
- 请根据数据分布统计来设定阈值，确保规则既不过于宽松也不过于严格
- 请确保覆盖所有检测到的风险聚类

## 示例规则
[
    {{
        "name": "高连接度欺诈团伙检测",
        "description": "检测度数异常偏高且某特征维度显著偏离的节点",
        "risk_type": "集团欺诈",
        "severity": "high",
        "conditions": [
            {{"field": "node_degree", "operator": ">", "value": 500}},
            {{"field": "feature_dim_0_zscore", "operator": ">", "value": 1.5}}
        ],
        "logic": "AND",
        "action": "review",
        "explanation": "基于 GNN Cluster-0: 高连接度+特征异常组合"
    }}
]

请生成规则 JSON 数组（直接输出 JSON，不要 ```json 标记）：
"""

# ============================================================
# 规则优化 Prompt: 基于沙盒回测结果优化规则
# ============================================================
RULE_OPTIMIZATION_PROMPT = """## 当前任务
以下规则在沙盒回测中表现不佳，请基于回测结果进行优化。

## 可用字段
| 字段名 | 说明 |
|--------|------|
| `node_degree` | 节点度数 |
| `node_degree_zscore` | 节点度数的标准分数 |
| `feature_dim_X` | 第 X 维原始特征值 |
| `feature_dim_X_zscore` | 第 X 维特征的标准分数 |

## 数据分布统计
{data_distribution}

## 待优化规则
{rules_to_optimize}

## 回测结果
{evaluation_results}

## 优化方向
- 如果召回率低 (< 50%): 放宽条件阈值或增加 OR 条件
- 如果误杀率高 (> 5%): 收紧条件阈值或增加 AND 条件
- 如果覆盖范围不足: 针对未覆盖的异常模式新增规则
- 参考数据分布统计调整阈值，找到正常与异常的分界点

## 历史有效规则参考
{historical_rules}

请输出优化后的规则 JSON 数组（直接输出 JSON，不要 ```json 标记）：
"""

# ============================================================
# 规则解释 Prompt: 为规则生成可读的风控报告
# ============================================================
RULE_EXPLANATION_PROMPT = """## 当前任务
请为以下风控规则生成面向运营人员的中文解释报告。

## 规则列表
{rules}

## 报告要求
对每条规则生成:
1. **风险场景**: 这条规则针对什么样的风险?
2. **检测逻辑**: 用通俗语言解释规则条件
3. **处置建议**: 触发规则后应如何处理
4. **注意事项**: 可能的误杀场景和豁免条件

请以 Markdown 格式输出：
"""
